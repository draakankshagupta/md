# Akanksha Gupta, MD — Website

Deployment source for AkankshaGuptaMD.com. The site is a static multi-page professional website prepared for Vercel.

The packaged static site is stored in `site.tar.gz`. Vercel extracts it during the build into the `dist` output directory.
